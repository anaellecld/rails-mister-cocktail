<div class="container">

  <h1>That is a <%= @cocktail.name %></h1>

  <!-- <div>
    <%= simple_form_for(@cocktail) do |f| %>
      <%= %>
      <%= %>
    <% end %>
  </div> -->

  <br>

  <%= link_to 'Add dose to cocktail', new_cocktail_dose_path(@cocktail), class: 'btn btn-primary' %>

  <br>

  <%= link_to 'Back', cocktails_path, class: 'btn btn-primary' %>
</div>
